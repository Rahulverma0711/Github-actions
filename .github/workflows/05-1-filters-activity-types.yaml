name: 05 - 1 - Event Filters and Activity Types

on:
    pull_request:
        types:
            - opened
            - synchronize

jobs:
    echo:
        runs-on: ubuntu-latest
        steps:
            - name: echo
              run: echo "Running whatever a PR is opened or Synchronized AND base branch is main"