name: 02 - Workflow Events 

on: 
    push:
    workflow_dispatch:
    pull_request:
    schedule:
        - cron: "*/5 * * * *"

jobs:
    echo:
        runs-on: ubuntu-latest
        steps:
            - name: echo hello
              run: |
                echo "hello running on ${{github.event_name}}"